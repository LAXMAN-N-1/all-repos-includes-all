import 'package:flutter/material.dart';



class VendorCategoriesScreen extends StatelessWidget {
  const VendorCategoriesScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text("Vendor Categories - Coming Soon")));
}

class VendorListScreen extends StatelessWidget {
  const VendorListScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text("Vendor List - Coming Soon")));
}

class VerifiedVendorsScreen extends StatelessWidget {
  const VerifiedVendorsScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text("Verified Vendors - Coming Soon")));
}

class VendorOnboardingScreen extends StatelessWidget {
  const VendorOnboardingScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text("Vendor Onboarding - Coming Soon")));
}
