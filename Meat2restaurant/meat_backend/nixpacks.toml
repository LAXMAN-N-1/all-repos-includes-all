[phases.setup]
nixPkgs = ["postgresql", "gcc", "python313"]

[phases.build]
cmds = ["python -m venv /app/.venv", "source /app/.venv/bin/activate", "pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
